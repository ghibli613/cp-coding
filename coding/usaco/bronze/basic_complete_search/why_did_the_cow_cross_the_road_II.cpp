#include<bits/stdc++.h>
using namespace std;

typedef long long ll;

void solve()
{
    string s; cin >> s;
    set<char> cows;
    for(int i = 0; i < 51; i++)
    {
        if(cows.find(s[i]) != cows.end())
        {
            for(int j = 0; j < 52; j++)
            {
                
            }
        }
    }
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int t = 1;
    // cin >> t;

    while(t--)
        solve();

    return 0;
}